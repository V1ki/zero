# ZeRo OS Web UI 设计规范

> **文档关系说明**：本文档定义 Web UI 的视觉风格、交互设计和页面规范。技术选型（框架、组件库）和命名规范以 [TechStack] 文档为准。本文档中的代码示例（CSS、API 路由）仅作设计说明用途，实际实现以 [TechStack - 命名规范] 和 [TechStack - Web UI 技术栈] 为准。

## 定位

Web UI 是 ZeRo OS 的控制面板 + 观测台，同时作为轻量 Channel。

- **主要功能**：观测系统状态、查看历史、管理配置
- **次要功能**：Web Channel（侧边抽屉形式的轻量对话）
- 飞书 / Telegram 是主力交互入口，Web UI 是辅助

---

## 视觉风格：Calm Futurism

安静的未来感——不是赛博朋克的霓虹喧嚣，而是深空望远镜控制台的气质。Linear 的克制 + Vercel 的质感 + 天文台控制室的氛围。

### 色彩系统

**背景层级（冷蓝灰调）**

| 层级 | 色值 | 用途 |
|------|------|------|
| 深层背景 | `#09090b` | 接近纯黑但偏冷 |
| 主背景 | `#0f1117` | 深邃墨蓝灰 |
| 卡片表面 | `#161b22` | 浮起感 |
| 浮层/弹出 | `#1c2333` | 最亮背景层 |

卡片用磨砂玻璃效果：

```css
background: rgba(22, 27, 34, 0.7);
backdrop-filter: blur(16px) saturate(1.2);
border: 1px solid rgba(255, 255, 255, 0.06);
```

**强调色（品牌色：冷青色）**

| 用途 | 色值 | 说明 |
|------|------|------|
| 主强调 | `#22d3ee` | cyan-400，系统"脉搏色" |
| 强调悬浮 | `#06b6d4` | cyan-500 |
| 强调暗淡 | `#0e7490` | cyan-700 |
| 强调光晕 | `rgba(34, 211, 238, 0.08)` | 背景辉光 |

**状态色**

| 状态 | 色值 | 说明 |
|------|------|------|
| 运行中/成功 | `#34d399` | emerald-400 |
| 警告/降级 | `#fbbf24` | amber-400 |
| 错误/熔断 | `#f87171` | red-400 |
| 空闲 | `#64748b` | slate-500 |

**文字层级**

| 层级 | 色值 | 说明 |
|------|------|------|
| 主要 | `#f1f5f9` | slate-100 |
| 次要 | `#94a3b8` | slate-400 |
| 弱化 | `#64748b` | slate-500 |
| 禁用 | `#475569` | slate-600 |

### 字体策略：双字体系统

```
显示/正文:  Geist (Vercel 出品)
            权重 400 (正文) / 600 (小标题) / 700 (大标题)

数据字体:   Geist Mono
            用于: Token 数字、Session ID、时间戳、日志、命令、YAML

中文回退:   "PingFang SC", "Noto Sans SC"
```

**字号体系**

| 用途 | 字号 | 字体/权重 | 备注 |
|------|------|-----------|------|
| 页面标题 | 20px | Geist 700 | |
| 区块标题 | 14px | Geist 600 | |
| 正文 | 13px | Geist 400 | |
| 辅助/标签 | 11px | Geist 400 | 字间距 +0.5px |
| 大数字指标 | 28px | Geist Mono 700 | 字间距 -0.5px |
| 代码/数据 | 13px | Geist Mono 400 | |

### 表面和材质

**卡片 hover 效果**

```css
.card:hover {
  border-color: rgba(34, 211, 238, 0.15);
  box-shadow: 0 0 20px rgba(34, 211, 238, 0.04);
}
```

**页面背景纹理**

```css
body {
  background-color: #09090b;
  background-image:
    radial-gradient(circle at 20% 50%, rgba(34, 211, 238, 0.03) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(34, 211, 238, 0.02) 0%, transparent 40%);
}
```

**输入框**

```css
.input {
  background: rgba(15, 17, 23, 0.8);
  border: 1px solid rgba(255, 255, 255, 0.08);
}
.input:focus {
  border-color: rgba(34, 211, 238, 0.4);
  box-shadow: 0 0 0 3px rgba(34, 211, 238, 0.08);
}
```

### 空间布局

| 元素 | 尺寸 |
|------|------|
| 侧边栏 | 220px 固定 |
| 主内容区 | 剩余空间，不限最大宽度 |
| Chat Drawer | 360px 宽，右侧滑出 |
| 卡片间距 | 16px |
| 卡片圆角 | 12px |
| 内边距 | 16-20px |

### 动画原则：有意义的运动

**页面进入 — 渐次浮现**

```css
.card {
  animation: fadeUp 0.4s ease-out forwards;
}
.card:nth-child(n) { animation-delay: calc(n * 60ms); }

@keyframes fadeUp {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: translateY(0); }
}
```

**实时数据 — Token 数字滚动**

数字更新时每位独立滚动，像机械翻牌器，让"系统正在消耗资源"可感知。

**活跃 Session — 呼吸脉搏**

```css
.status-active {
  animation: pulse 2.5s ease-in-out infinite;
}
@keyframes pulse {
  0%, 100% { box-shadow: 0 0 6px rgba(52, 211, 153, 0.4); }
  50%      { box-shadow: 0 0 12px rgba(52, 211, 153, 0.6); }
}
```

**Chat Drawer — 侧滑 + 磨砂遮罩**

```css
.drawer-panel {
  animation: slideIn 0.3s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}
```

Apple 风格弹性缓出曲线。

**不做动画的地方**

- 页面切换（直接渲染，不要过渡延迟）
- 表格排序/筛选（数据变更要即时）
- Hover 状态（`0.15s ease` 足够）

---

## 侧边栏

所有页面共享的固定侧边栏。

```
┌──── 220px ────┐
│               │
│   ZeRo        │   ← 标志/名称，14px Geist 700
│               │
│   Dashboard   │   ← 当前页高亮：左侧 2px cyan-400 边框
│   Sessions    │       + 文字变为 slate-100
│   Memory      │       非当前页：slate-400 文字
│   Memo        │
│   Logs        │
│   Config      │
│   Metrics     │
│               │
│               │   ← 弹性空白
│               │
│   ● Running   │   ← 系统状态微缩版
│   claude-opus │
│               │
│   💬          │   ← Chat Drawer 触发按钮
│               │
└───────────────┘
```

底部两个元素：系统状态微缩指示（一眼知道系统活着且用的什么模型）和 Chat 按钮。无论在哪个页面，都能感知系统状态并发起对话。

---

## Dashboard 页面

### 信息层级（从上到下，从紧急到背景）

1. 系统还活着吗？（0.5 秒确认）
2. 有没有需要处理的？（1 秒扫到）
3. 现在在干什么？（活跃 Session）
4. 刚才发生了什么？（事件流）
5. 花了多少钱？（费用概览）

### A. 系统状态栏

页面顶部窄带（40px），正常时几乎隐形，异常时醒目：

```
正常:  ● Running    claude-opus    Uptime 3d 4h    3 sessions active
降级:  ◐ Degraded   claude-sonnet (降级自 opus)    ← 黄色
熔断:  ◉ Fused      等待人工介入                    ← 红色，脉搏加快
```

### B. 需要关注

从备忘录"需要用户处理"和系统通知提取的待办项。**没有待办时不显示**，有待办时浮现：

```
┌─ 需要关注 ────────────────────────────────────┐
│  ⚠  Telegram Bot Token 还未提供                │
│     来自备忘录 · 2 天前                         │
│  ⚠  claude-opus API 连续 3 次超时，已降级       │
│     自动降级到 claude-sonnet · 10 分钟前         │
└────────────────────────────────────────────────┘
```

卡片边框用 `amber-400` 微弱光晕，第一眼就能注意到。

### C. 费用概览

```
┌─ 费用概览 ──────────────────────────────────────┐
│   Today           This Week        This Month    │
│   $1.24           $8.67            $32.15        │
│   18.4k tokens    142k tokens      1.2M tokens   │
│                                                  │
│   claude-opus    ████████████░░  $0.89    72%    │
│   claude-sonnet  █████░░░░░░░░░  $0.31    25%    │
│   gpt-4o         █░░░░░░░░░░░░░  $0.04     3%    │
└──────────────────────────────────────────────────┘
```

- 大数字用 28px Geist Mono 700
- 数字更新时翻牌滚动动画
- 条形图全部用 `cyan-400`，通过长度区分
- 实时更新（WebSocket 推送）

### D. 活跃 Sessions

```
┌─ Active Sessions ──────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ ●  sess_001                                               │ │
│  │    飞书 · claude-opus · 开始于 10:00                       │ │
│  │    重构 Provider Adapter                                   │ │
│  │    ▸ bash  git diff --stat                    just now    │ │
│  │    ▸ edit  src/provider.ts                    2 min ago   │ │
│  └───────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

- 活跃 Session（`●`）有脉搏动画，空闲（`◌`）无
- 工具调用实时更新，新调用从底部淡入
- 工具图标 `▸` 用 `slate-500`，工具名用 `cyan-400`
- 点击跳转到 Session 详情页

### E. 事件流

```
┌─ Recent Activity ──────────────────────────────────────────────┐
│  10:05   sess_001  ▸ bash    git commit -m "refactor adapter"  │
│  10:04   sess_001  ▸ edit    src/provider.ts L42-58            │
│  09:00   sess_003  ▸ sched   weekly_report 触发                │
│  08:30   system    ▸ alert   claude-opus API 超时，降级到 sonnet│
│                                          查看全部 →             │
└────────────────────────────────────────────────────────────────┘
```

- 单行时间线，信息密度高
- 新事件从顶部滑入（`translateY(-12px) → 0`）
- 系统级事件用状态色高亮
- 实时更新（WebSocket）

### 动态布局

Dashboard 根据系统状态自适应：

- 一切正常时：B 卡片不出现，页面简洁
- 有待办时：B 和 C 并排
- 系统异常时：B 提升到全宽，状态栏变色

---

## Sessions 页面

### 列表视图

```
┌─ Sessions ─────────────────────────────────────────────────────┐
│  ┌─ 筛选栏 ─────────────────────────────────────────────────┐  │
│  │  全部 · 飞书 · Telegram · Scheduler · Web    🔍 搜索     │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ● sess_001         重构 Provider Adapter                │   │
│  │  飞书 · claude-opus · 今天 10:00 - 10:45                 │   │
│  │  12 次工具调用 · 3.2k tokens · $0.24                     │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  ● sess_002         部署 Telegram Bot                    │   │
│  │  Telegram · claude-sonnet · 今天 09:30 - 进行中          │   │
│  │  8 次工具调用 · 1.8k tokens · $0.09                      │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────┘
```

每条 Session 三行：

1. 状态灯 + ID + 任务描述（视觉焦点）
2. 来源 · 模型 · 时间范围
3. 工具调用次数 · Token · 费用（判断"规模"）

### Session 详情页

```
┌─ Session Detail ───────────────────────────────────────────────┐
│  ← Sessions    sess_001                                        │
│  ┌─ 元信息栏 ───────────────────────────────────────────────┐  │
│  │  重构 Provider Adapter                                    │  │
│  │  飞书 · claude-opus · 今天 10:00 - 10:45 (45min)         │  │
│  │  12 calls · 3.2k tokens (2.1k in / 1.1k out) · $0.24    │  │
│  │  ┌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌┐   │  │
│  │  ╎  ●━━━━━━━━●━━━●━━━━●━●━━━━━━━━●━━━●━━━━━━●  ╎   │  │
│  │  ╎  10:00                10:23↑降级          10:45  ╎   │  │
│  │  └╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌┘   │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌─ 时间线 ─────────────────────────────┬─ 上下文面板 ──────┐  │
│  │  (对话 + 工具调用 + 决策事件 +        │  (默认 Summary /   │  │
│  │   Task Closure 时间线)                │   Trace；选中项时   │  │
│  │                                       │   显示详情)         │  │
│  └───────────────────────────────────────┴───────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

#### 时间轴 minimap

- 细线上的节点代表工具调用
- 节点颜色区分类型（bash=cyan, read/edit=slate, write=emerald, fetch=amber）
- 点击节点跳转到时间线对应位置
- 模型降级、错误用三角标记

#### 时间线主体（左侧 65%）

四种消息块视觉区分：

```
User 消息:
  背景: rgba(34, 211, 238, 0.06)
  左边框: 2px solid cyan-400

Agent 消息:
  背景: transparent
  文字: slate-200

工具调用块:
  背景: rgba(255, 255, 255, 0.03)
  边框: 1px solid rgba(255, 255, 255, 0.06)
  默认折叠输出，点击展开

决策块:
  背景: rgba(255, 255, 255, 0.03)
  边框: 1px solid rgba(34, 211, 238, 0.12)
  用于展示 context_compression / memory_retrieval / tool_selection
```

工具调用块结构：

```
┌─ [工具图标] [工具名] ─── [状态] ──── [耗时] ───┐
│  [主要内容：文件路径/命令/URL]                    │
│  ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄ (可展开分隔线) ┄┄┄┄┄┄┄┄┄┄  │
│  [输出预览，默认折叠]                             │
│  + N 行                                          │
└──────────────────────────────────────────────────┘
```

状态：`✓` 成功（emerald-400）、`✗` 失败（red-400）、`⟳` 进行中（脉搏动画）

系统事件（降级/熔断）：

```
┌─ ⚠ system ──────────────────────────────────────┐
│  claude-opus API 超时，自动降级到 claude-sonnet   │
└──────────────────────────────────────────────────┘
```

全宽，左边框和文字用状态色，非常醒目。

Task Closure 不并入通用 decision lane，仍保留独立块，避免和已有 closure 详情重复。

#### 上下文面板（右侧 35%）

默认显示 Session 摘要：

- Summary（AI 生成的总结）
- Model Usage（各模型 token 消耗）
- Tool Calls（工具调用分布条形图）
- Files Touched（涉及的文件列表）
- Related Memory（关联记忆链接）

默认面板带 `Summary / Trace` 两个 tab：

- `Summary`：Session 摘要、模型用量、工具分布、关联记忆等。
- `Trace`：按区块展示 `Decisions`、`Task Closure`、`Full Trace`。

点击时间线中的不同块时，右侧切换为对应详情：

- 工具块：输入参数 + 输出结果
- 决策块：`decision_type` / `outcome` / `source_kind` / `duration` / `rationale` / `context` / `detail`
- Task Closure：`action` / `reason` / classifier 请求与响应

#### 实时 Session

- 新消息从底部出现，带淡入动画
- Agent 回复有 streaming 效果
- 工具调用进行中显示 `⟳` + 脉搏边框
- 自动滚动到底部（用户上翻时停止，显示"跳到最新 ↓"）

#### 排队消息的展示

当用户在 Agent 执行任务期间发来消息（详见 [Architecture - 消息排队]），时间线上需要特殊标识：

**排队确认**：用户发送消息后，如果 Agent 正忙，消息气泡正常显示，下方追加一行系统提示：

```
User 消息:
┌──────────────────────────────────────────────────────┐
│  现在处理得怎么样了                                    │
│  ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄  │
│  ⏳ 消息已排队，当前工具执行完毕后处理                  │
└──────────────────────────────────────────────────────┘
```

提示行用 `slate-500` 文字 + `⏳` 图标，不抢视觉焦点。当排队消息被 Agent 实际处理后，提示行自动消失。

**Agent 对排队消息的回应**：Agent 回应排队消息时，其回复在时间线上和正常回复视觉一致，但在回复气泡上方插入一条小标签：

```
   ┌─ 回应排队消息 ─────────────────────────────────────┐
   │  ↳ 对 "现在处理得怎么样了" 的回应                    │
   └───────────────────────────────────────────────────┘
   Agent 消息:
   目前已经完成了 git diff 分析，发现需要改 3 个文件...
```

标签用 `slate-600` 背景 + 小字体（12px），引用用户原消息的前 30 字符。这样回放时能清楚看到"这段回复是因为用户问了进度"。

**时间轴 minimap 上的标记**：排队消息在 minimap 上用一个小三角标记（`slate-400`，向下箭头），hover 显示消息预览。和模型降级/错误的三角标记形状相同但颜色不同，视觉语言一致。

#### 键盘导航

| 快捷键 | 动作 |
|--------|------|
| `j` / `k` | 下一个/上一个工具调用 |
| `Enter` | 展开/折叠工具输出 |
| `Esc` | 右侧面板回到 Summary |
| `g g` / `G` | 跳到顶部/底部 |

---

## Memory 页面

### 设计思路

Memory 和 Session 的使用方式完全不同。Session 是线性的时间线回放，Memory 是**非线性的知识网络**。核心挑战：记忆数量会越来越多，类型多样（session/incident/runbook/decision/note），而且彼此之间有关联。需要**搜索能力 + 结构化浏览 + 关联可导航**。

### 页面结构

左右分栏（master-detail 同屏）：左侧列表 40%，右侧详情 60%。

和 Session 页面不同，Memory 用左右分栏而不是列表→详情的页面跳转——浏览记忆时经常需要快速扫多条，左右分栏减少页面跳转的打断感。右侧占比更大是因为记忆内容本身（Markdown 文档）需要更多阅读空间。

```
┌─ Memory ───────────────────────────────────────────────────────┐
│  ┌─ 搜索 + 过滤 ────────────────────────────────────────────┐  │
│  │  🔍 搜索记忆...                                    ⌘ K   │  │
│  │  类型: 全部 · incident · runbook · decision · note        │  │
│  │  状态: 全部 · verified · draft · conflict                 │  │
│  │  排序: 相关度 · 最近更新 · 最近创建 · confidence ↓        │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌─ 记忆列表 ──────────────────────┬─ 记忆详情 ──────────────┐ │
│  │                                  │                         │ │
│  └──────────────────────────────────┴─────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

### 搜索交互

- **语义搜索**（默认）：自然语言输入，走 Embedding 向量检索
- **精确搜索**：引号包裹时全文匹配，如 `"EADDRINUSE"`
- 输入即搜索，300ms 防抖
- 全局快捷键 `⌘ K`（在任何页面按下跳转到 Memory 搜索）

### 过滤器

类型颜色（全站统一）：

| 类型 | 颜色方案 |
|------|----------|
| incident | `red-400`/10% 背景 + `red-400` 文字 |
| runbook | `emerald-400`/10% + `emerald-400` |
| decision | `amber-400`/10% + `amber-400` |
| note | `blue-400`/10% + `blue-400` |
| session | `slate-400`/10% + `slate-300` |

过滤器默认折叠成一行紧凑显示，只有在需要精细筛选时才展开完整选项。大多数时候搜索框就够用了。

```
折叠状态:
┌──────────────────────────────────────────────────────────┐
│  🔍 搜索记忆...          类型: 全部  状态: 全部  ⌘ K  │
└──────────────────────────────────────────────────────────┘

展开状态 (点击任一过滤项):
┌──────────────────────────────────────────────────────────┐
│  🔍 搜索记忆...                                  ⌘ K  │
│                                                        │
│  类型  全部 · incident · runbook · decision · note     │
│  状态  全部 · verified · draft · conflict              │
│  排序  相关度 ↓ · 最近更新 · 最近创建 · confidence     │
└──────────────────────────────────────────────────────────┘
```

### 记忆列表

每条记忆卡片三行：

```
第一行:  [类型标签]  [标题]
第二行:  [confidence 指示器]  [状态标签]  [时间]
第三行:  [tags]
```

**Confidence 指示器**：5 个圆点，填充数量代表 confidence 区间：

| 显示 | 区间 | 颜色 | 含义 |
|------|------|------|------|
| `●●●●●` | 0.8 - 1.0 | `emerald-400` | 可用于自动修复 |
| `●●●●○` | 0.6 - 0.8 | `amber-400` | |
| `●●●○○` | 0.4 - 0.6 | | |
| `●●○○○` | 0.2 - 0.4 | | |
| `●○○○○` | 0.0 - 0.2 | `red-400` | 低可信度 |

**状态标签**：

| 状态 | 样式 |
|------|------|
| `verified` | `emerald-400` 文字 |
| `draft` | `slate-400` 文字 |
| `conflict` | `red-400` 文字 + 微弱红色背景（需要关注） |
| `archived` | `slate-600` 文字（极弱化） |

**Tags**：`slate-500` 文字，`#` 前缀，不加背景框——保持轻量，tags 是辅助信息不需要视觉重量。

#### 列表交互

- 选中某条记忆时，左边框亮起 `cyan-400`，右侧面板加载详情
- 键盘导航：`j/k` 上下移动，`Enter` 选中
- 滚动加载，不做分页——记忆条目通常不会太多（几百条），虚拟列表足够

### 记忆详情面板

四个区块：

**头部** — 类型、标题、元数据。元数据用一个紧凑的 2×2 网格展示（状态/confidence/创建时间/更新时间/来源），背景用比面板更深一层的色块，视觉上像一个"信息卡"嵌在面板里。

**内容** — Markdown 渲染。这是面板的主体，占最多空间。代码块、列表、标题按标准 Markdown 样式渲染。

**关联** — 这条记忆的上下文网络：

- **来源 Session**：点击跳转到 Session 回放页面
- **关联记忆**：`related` 字段指向的其他记忆，点击直接在左侧列表中定位并加载
- **Tags**：点击某个 tag 等于在搜索框里输入该 tag 做过滤

关联记忆的跳转是这个页面最重要的交互之一——它让"平铺列表"变成了"可导航的知识网络"。点击一条关联记忆 → 左侧列表高亮该条 → 右侧加载新内容 → 新内容里又有新的关联 → 继续探索。

**操作栏** — 底部固定，三个操作按钮：

| 操作 | 说明 |
|------|------|
| 归档 | 将这条记忆移入 `archive/`，从主列表中隐藏 |
| 删除 | 彻底删除（需二次确认） |
| 在文件中打开 | 打开对应的 `.md` 文件路径 |

按钮用 ghost 样式（无背景，hover 时出现淡色背景），删除按钮 hover 时变红。

### 编辑模式

点击头部的"编辑"按钮后，详情面板切换为编辑模式：

- **标题**：变为可编辑输入框
- **状态**：下拉选择（verified / draft / conflict）
- **Confidence**：滑块，0-1，步长 0.05，右侧显示精确数值
- **内容**：CodeMirror Markdown 编辑器，支持语法高亮。不做分屏预览——编辑区本身就有 Markdown 高亮，够了
- **Tags**：胶囊式标签编辑，`×` 删除，`+` 添加

编辑模式和阅读模式之间的切换用平滑过渡——输入框从文字位置"展开"出来，不是整个面板重绘。

### 空状态

**无搜索结果**：

```
没有找到匹配的记忆
试试更宽泛的关键词，或减少过滤条件
```

**未选中记忆（初始状态）**：

右侧显示 Memory Overview 统计：

```
┌─ Memory Overview ──────────────────────────────────────────────┐
│                                                                 │
│  incident      12 条                                            │
│  runbook        8 条                                            │
│  decision       5 条                                            │
│  note          23 条                                            │
│  session       87 条                                            │
│                                                                 │
│  总计 135 条                                                    │
│  verified 98 · draft 31 · conflict 6                            │
│                                                                 │
│  最近更新: 2 小时前                                              │
│  存储占用: 2.4 MB                                               │
│                                                                 │
│  ─────────────────                                              │
│                                                                 │
│  conflict 6 条                                                  │
│  有 6 条记忆存在冲突，需要人工裁决                                 │
│  查看冲突 →                                                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

底部的 conflict 提示是主动引导——鼓励用户去处理冲突，而不是让它们静默积累。"查看冲突 →" 点击后等于在过滤器里选中 `状态: conflict`。

### Memory 动画

- **列表项进入**：渐次浮入（`fadeUp`，间隔 30ms）
- **详情面板切换**：crossfade（旧内容淡出 100ms → 新内容淡入 150ms）
- **关联记忆跳转**：左侧列表平滑滚动，目标项强调色边框闪烁（`flash` 动画 400ms）
- **Confidence 滑块**：拖动时圆点颜色实时变化（绿→黄→红）

```css
@keyframes flash {
  0%   { border-color: rgba(34, 211, 238, 0.6); }
  100% { border-color: rgba(34, 211, 238, 0.15); }
}
```

---

## Memo 页面

备忘录是 AI 和人类之间的**活文档**。打开这个页面的场景：想看 AI 现在在做什么/打算做什么，想调整优先级给 AI 指方向，想看有没有需要自己处理的事项。

核心是**直接编辑**，不是阅读→点编辑→切模式。打开就是编辑器。

### 页面结构

```
┌─ Memo ─────────────────────────────────────────────────────────┐
│                                                                 │
│  ┌─ 编辑器 ────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  # 备忘录                                                │   │
│  │                                                          │   │
│  │  ## 目标                                                 │   │
│  │  - 本周完成 v2.0 发布                                    │   │
│  │  - Channel 模块重构，计划下周开始                         │   │
│  │                                                          │   │
│  │  ## 需要用户处理                                          │   │
│  │  - Telegram Bot Token 还未提供                            │   │
│  │  - v2.0 changelog 需要人工审核                            │   │
│  │                                                          │   │
│  │  ---                                                     │   │
│  │                                                          │   │
│  │  ### Coder Agent                                         │   │
│  │  **进行中**：重构 Provider Adapter，预计今天完成           │   │
│  │  **计划**：完成后跑单元测试，通过则提交 PR                │   │
│  │                                                          │   │
│  │  ### Ops Agent                                           │   │
│  │  **进行中**：部署 Telegram Bot，等待用户提供 Token        │   │
│  │  **计划**：部署完成后自动跑回归测试                       │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─ 底部状态栏 ────────────────────────────────────────────┐   │
│  │  最后编辑: Coder Agent · 3 分钟前    自动保存 ✓           │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 设计决策

**全页编辑器，无分栏**。Memo 就是一个文件，不需要列表→详情的导航。CodeMirror + Markdown 高亮，打开即编辑，`⌘ S` 保存（也支持自动保存，5 秒防抖）。

**编辑器样式**和 Memory 编辑模式的 CodeMirror 保持一致——同样的字体（Geist Mono 13px）、同样的 Markdown 语法高亮配色。

**Agent 分区的视觉区分**。虽然底层是纯 Markdown，编辑器里可以在 `### Agent` 标题处加一条极淡的左侧色条（`cyan-400` 10% 不透明度），视觉上暗示"这是 Agent 的区域"。人类可以编辑任何位置，色条只是辅助认知，不是限制。

**实时协作提示**。如果 AI 正在编辑这个文件（通过文件锁检测），底部状态栏显示：

```
┌──────────────────────────────────────────────────────────────┐
│  ⟳ Coder Agent 正在编辑...    最后保存: 3 分钟前              │
└──────────────────────────────────────────────────────────────┘
```

不做实时协同编辑（不是 Google Docs），但要让用户知道"AI 也在改这个文件"。如果检测到冲突（用户保存时文件已被 AI 修改），弹出简单的 diff 确认。

**底部状态栏**：40px，固定底部。显示最后编辑者（人类还是哪个 Agent）、时间、保存状态。信息密度低，不需要抢注意力。

---

## Logs 页面

给开发者和高级用户看的——需要排查问题时才来。信息密度要高，交互要快。

### 页面结构

```
┌─ Logs ─────────────────────────────────────────────────────────┐
│  ┌─ 过滤栏 ────────────────────────────────────────────────┐   │
│  │  类型  events · trace · requests · snapshots             │   │
│  │  级别  info · warn · error        🔍 过滤...    ⌘ F     │   │
│  │  时间  [  最近 1 小时  ▾ ]                               │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌─ 日志表格 ──────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  时间        级别  Session      事件         摘要         │   │
│  │  ─────────────────────────────────────────────────────── │   │
│  │  10:05:23    info  sess_001    tool_call    bash ls -la  │   │
│  │  10:05:20    info  sess_001    span_end     tool:bash    │   │
│  │  10:04:58    warn  system      api_timeout  opus 超时    │   │
│  │  10:04:55    info  sess_001    llm_request  claude-son.. │   │
│  │  10:04:30    info  sess_002    snapshot     context_compr │   │
│  │  ...                                                     │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌─ 详情抽屉（点击行展开）─────────────────────────────────┐   │
│  │  {                                                       │   │
│  │    "ts": "2026-02-27T10:05:23Z",                         │   │
│  │    "event": "tool_call",                                 │   │
│  │    "tool": "bash",                                       │   │
│  │    "outputSummary": "listed 12 files",                   │   │
│  │    "durationMs": 45                                      │   │
│  │  }                                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 设计决策

**表格为主**。日志是结构化数据，表格是最高效的呈现方式。每行单行显示，Geist Mono 13px，行高紧凑（32px）。表格全宽，不分栏——需要最大水平空间来展示信息。

**分层记录切换**。第一阶段不合并文件，Logs 页面需要同时容纳全局事件流、Session Trace 和 specialized ledgers：

| 来源 | 说明 | 显示列 |
|------|------|--------|
| `events` | 全局系统事件流（默认，最常用） | event / tool / outputSummary |
| `trace` | 当前 Session 的 span 执行追踪 | kind / name / duration / parent |
| `requests` | 当前 Session 的 LLM 请求台账 | model / tokens / cost |
| `snapshots` | 当前 Session 的上下文快照台账 | trigger / tools / messages_before |

切换时表格列动态变化。全局 Logs 入口默认落在 `events` 视图；进入某个 Session 后，再暴露 `trace`、`requests`、`snapshots` 这些 Session 级记录。当前这些 Session 视图优先从 `trace.jsonl` 投影，旧 ledger 文件只作为兼容 fallback。Decision 视图暂不在全局 Logs 页单独展开，而是在 Session Detail 的 Trace tab 中展示。

**级别用颜色标记**。`error` 行整行微弱红色背景（`red-400` 5% 不透明度），`warn` 用 `amber-400` 5%，`info` 无背景。在密集的日志表格里，颜色是最快的扫描锚点。

**时间范围**。默认"最近 1 小时"，可选"最近 24 小时"、"最近 7 天"、"自定义范围"。日志量可能很大，不预加载全量。

**行展开详情**。点击某行，下方原位展开一个面板（不是跳转、不是侧边栏），显示该条日志的完整 JSON。JSON 语法高亮，代码块样式和全站一致。`Esc` 或再次点击收起。

**实时尾部追踪**。右上角 `▶ Live` 开关。打开时新日志实时从底部推入（WebSocket），自动滚动。行为像 `tail -f`——开发者最熟悉的模式。用户上翻时自动暂停追踪，出现"跳到最新 ↓"。

**搜索过滤**。`⌘ F` 聚焦到过滤输入框，支持对任意列做文本过滤。输入即过滤，200ms 防抖。

---

## Config 页面

管理三块配置：模型注册表、Scheduler、熔断名单。对应 `config.yaml` 和 `fuse_list.yaml`。

所有配置操作均可在 UI 上完成（完整 CRUD），底层读写 `config.yaml` / `fuse_list.yaml`。每次保存自动纳入 Git 版本控制，支持回滚。

### 页面结构

```
┌─ Config ───────────────────────────────────────────────────────┐
│  ┌─ Tab 栏 ────────────────────────────────────────────────┐   │
│  │  Models    Scheduler    Fuse List                        │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌─ 内容区 ────────────────────────────────────────────────┐   │
│  │  (根据选中 Tab 显示对应配置)                              │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────┘
```

### 保存机制（三个 Tab 共用）

所有编辑操作遵循统一的保存流程：

1. 用户在 UI 上修改配置后，点击「保存」或 `⌘ S`。
2. 后端校验配置合法性（如 cron 表达式格式、模型 ID 有效性）。
3. 校验通过后写入 `config.yaml` / `fuse_list.yaml`，触发 Git 自动 commit。
4. 需要生效的变更（如 Scheduler 的 cron 变更）同步更新到运行时。
5. 校验失败时在对应字段下方显示内联错误提示（`red-400` 文字），不阻塞其他字段编辑。

底部状态栏（和 Memo 页面一致，40px 固定底部）显示保存状态：

```
┌──────────────────────────────────────────────────────────────┐
│  已保存 ✓ · 30 秒前                            撤销上次修改  │
└──────────────────────────────────────────────────────────────┘

未保存时：
┌──────────────────────────────────────────────────────────────┐
│  有未保存的修改                              取消  ·  保存 ⌘S │
└──────────────────────────────────────────────────────────────┘
```

「撤销上次修改」等价于 `git revert` 最近一次 config commit，需二次确认。

### Models Tab

按 Provider 分组展示模型注册表，支持完整的增删改操作。

```
┌─ Models ───────────────────────────────────────────────────────┐
│                                                                 │
│  ┌─ 运行时状态 ─────────────────────────────────────────────┐  │
│  │  活跃模型  claude-opus (anthropic)              [切换 ▾]  │  │
│  │  降级链    ① claude-opus → ② claude-sonnet → ③ gpt-4o    │  │
│  │            [编辑降级链]                                    │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─ anthropic ────────────────────────────── [编辑] [删除] ─┐  │
│  │  api_type: anthropic_messages                             │  │
│  │  base_url: https://api.anthropic.com                      │  │
│  │  认证: API Key · ● 已配置                                 │  │
│  │                                                           │  │
│  │  ┌──────────────────┬───────────────────────── [编辑] ─┐  │  │
│  │  │  claude-opus     │  claude-opus-4-6                 │  │  │
│  │  │  ● 活跃          │  200k ctx · 32k out              │  │  │
│  │  │                  │  tools, vision, reasoning         │  │  │
│  │  │                  │  tags: powerful                   │  │  │
│  │  ├──────────────────┼───────────────────────── [编辑] ─┤  │  │
│  │  │  claude-sonnet   │  claude-sonnet-4-5-20250929      │  │  │
│  │  │  ◌ 备选          │  200k ctx · 8k out               │  │  │
│  │  │                  │  tools, vision                    │  │  │
│  │  │                  │  tags: fast, balanced             │  │  │
│  │  └──────────────────┴──────────────────────────────────┘  │  │
│  │                                         [+ 添加模型]      │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─ openai ──────────────────────────────── [编辑] [删除] ─┐  │
│  │  ...                                                      │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  [+ 添加 Provider]                                              │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

#### 运行时状态区

页面顶部的运行时状态区是全局视角的快捷操作入口：

**切换活跃模型**：点击「切换 ▾」展开下拉菜单，列出所有已注册模型。选中后立即生效（更新当前 Session 使用的模型），同时写入 config。下拉菜单按 Provider 分组，每项显示模型名 + 状态指示（可达/不可达）。

**编辑降级链**：点击「编辑降级链」展开内联编辑区。降级链用**拖拽排序**——每个模型是一个胶囊标签，拖动调整顺序。可从"可用模型"列表中拖入新模型，或将现有模型拖出移除。

```
编辑降级链（拖拽排序）:
┌──────────────────────────────────────────────────────────┐
│  ① [claude-opus ×]  ② [claude-sonnet ×]  ③ [gpt-4o ×]  │
│                                                          │
│  可用模型:  [deepseek-r1]  [o3]                          │
│                                          取消  ·  保存   │
└──────────────────────────────────────────────────────────┘
```

#### Provider 操作

**添加 Provider**：点击底部「+ 添加 Provider」，展开内联表单。表单根据选择的认证方式动态切换凭证输入区：

```
┌─ 添加 Provider ──────────────────────────────────────────────┐
│  名称         [                    ]                          │
│  协议类型     [Anthropic Messages ▾]                          │
│               可选: Anthropic Messages / OpenAI Chat          │
│               Completions / OpenAI Responses                  │
│  Base URL     [https://api.anthropic.com    ]                 │
│                                                               │
│  认证方式     ○ API Key  ○ OAuth Token  ○ ChatGPT OAuth       │
│                                                               │
│  ┄┄┄┄┄┄┄┄ 凭证区（根据认证方式切换）┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄   │
│                                                               │
│  [API Key 模式]                                               │
│  API Key      [                    ]    存入保密箱             │
│                                                               │
│  [OAuth Token 模式]                                           │
│  OAuth Token  [                    ]    存入保密箱             │
│  ℹ 粘贴 Anthropic OAuth Token（sk-ant-oat- 开头）             │
│                                                               │
│  [ChatGPT OAuth 模式]                                         │
│  [🔗 通过 ChatGPT 登录授权]                                   │
│  ℹ 点击后打开浏览器窗口完成 OAuth 登录                        │
│                                                               │
│                                           取消  ·  添加       │
└───────────────────────────────────────────────────────────────┘
```

- 协议类型下拉选择三种之一（详见 [Architecture - Provider Adapter]）
- 选择协议类型后自动填充默认 Base URL，用户可覆盖
- **认证方式**用 Radio 切换（详见 [Architecture - 认证方式]）：
  - `API Key`：粘贴输入，存入保密箱。Anthropic 协议使用 `x-api-key`，OpenAI 兼容协议使用 `Authorization: Bearer`
  - `OAuth Token`：粘贴输入（`sk-ant-oat-` 前缀），存入保密箱。系统通过 Anthropic SDK 的 `authToken` 参数注入认证并附加 Beta Headers（详见 [TechStack - Anthropic OAuth Token]）。适用于 Anthropic OAuth Token
  - `ChatGPT OAuth`：点击按钮打开浏览器 OAuth 授权页面。用户完成登录后，系统通过回调自动获取 Token 并存入保密箱。按钮在授权完成前显示「🔗 通过 ChatGPT 登录授权」，完成后切换为「✓ 已授权」（`emerald-400`）+ 「重新授权」链接
- API Key / OAuth Token 输入后显示为 `●●●●●●` 遮蔽，旁边显示凭证引用名
- 表单校验：名称不可重复，Base URL 格式校验，ChatGPT OAuth 模式下必须完成授权后才能点击「添加」

**编辑 Provider**：点击 Provider 卡片右上角「编辑」，卡片头部区域原位切换为可编辑表单（和 Memory 编辑模式的过渡方式一致——输入框从文字位置展开，不重绘整个卡片）。可修改 Base URL 和凭证。协议类型、认证方式和名称创建后不可修改（变更这些等价于删除重建）。

**删除 Provider**：点击「删除」，弹出确认对话框，显示该 Provider 下所有模型列表以及受影响的降级链。如果降级链中包含该 Provider 的模型，需先从降级链中移除。确认后删除 Provider 及其下所有模型。

#### 模型操作

**添加模型**：点击 Provider 卡片内「+ 添加模型」，在模型列表底部展开内联表单：

```
┌─ 添加模型 ────────────────────────────────────────────────────┐
│  别名           [                    ]    如 claude-opus       │
│  Model ID       [                    ]    如 claude-opus-4-6   │
│  Max Context    [200000    ]                                   │
│  Max Output     [32000     ]                                   │
│  Capabilities   [tools] [vision] [reasoning] [+ 添加]          │
│  Tags           [powerful] [+ 添加]                            │
│  Pricing        □ 自定义价格（不勾选则使用 litellm 默认值）     │
│                                                                │
│                                            取消  ·  添加       │
└────────────────────────────────────────────────────────────────┘
```

- Capabilities 和 Tags 用胶囊标签，`×` 删除，`+` 添加
- Pricing 默认折叠（大多数模型不需要自定义），勾选后展开 input/output/cache_write/cache_read/reasoning 五个价格字段
- 别名不可与同 Provider 下其他模型重复

**编辑模型**：点击模型行右侧「编辑」，该行展开为可编辑表单（和添加表单结构相同，预填当前值）。别名创建后不可修改。

**删除模型**：模型行 hover 时右侧出现 `×`，点击需确认。如果模型在降级链中或是当前活跃模型，提示用户先调整。

#### 凭证状态指示

每个 Provider 卡片头部显示认证方式和凭证状态。格式为"认证: {方式} · {状态}"：

| 认证方式 | 状态 | 显示 | 说明 |
|---------|------|------|------|
| 所有 | `● 已配置` | `emerald-400` | 保密箱中有对应凭证 |
| 所有 | `○ 未配置` | `red-400` | 缺少凭证，该 Provider 下所有模型不可用 |
| 所有 | `● 已配置 · 不可达` | `amber-400` | 有凭证但健康检查失败（网络问题或 Base URL 错误） |
| OAuth | `● 已配置 · 已过期` | `amber-400` | Token 过期（健康检查 401/403），需重新授权 |
| ChatGPT OAuth | `⟳ 授权中...` | `cyan-400` | OAuth 浏览器窗口已打开，等待用户完成授权 |

点击状态标签的行为根据认证方式不同：

- **API Key / OAuth Token**：弹出凭证编辑弹窗（预填遮蔽值），可更新或清除
- **ChatGPT OAuth**：弹出弹窗显示"重新授权"按钮，点击后重新发起 OAuth 流程

当系统通过 WebSocket 收到 `credential:expired` 事件时，对应 Provider 卡片的凭证状态自动切换为"已过期"，并在卡片顶部显示一条内联警告："凭证已过期，请重新配置"（`amber-400` 背景横幅）。

#### 模型健康检查

Models Tab 加载时和每次保存后，自动对所有配置了凭证的 Provider 执行一次轻量健康检查（调用 Provider Adapter 的 `healthCheck()`，详见 [TechStack - Provider Adapter 接口]）。健康检查同时验证网络可达性和凭证有效性。结果显示在 Provider 卡片上：

- 检查中：Provider 名称旁边显示 `⟳` 旋转图标
- 可达且凭证有效：不显示额外标记（干净状态）
- 不可达：Provider 名称旁边显示 `⚠ 不可达`（`amber-400`），hover 显示错误详情
- 凭证失效（401/403）：凭证状态切换为「已过期」，提示用户重新配置

### Scheduler Tab

展示和管理定时任务，支持完整的增删改。

```
┌─ Scheduler ────────────────────────────────────────────────────┐
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  daily_update_check                    启用 ◉  [编辑 ▾] │   │
│  │  每天 02:00  ·  claude-sonnet                            │   │
│  │  "检查系统更新，有更新则执行升级"                         │   │
│  │  上次执行: 今天 02:00 · ● 成功 · 45s                     │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  weekly_report                         启用 ◉  [编辑 ▾] │   │
│  │  每周一 09:00  ·  默认模型                                │   │
│  │  "整理上周的 sessions 和 incidents，生成周报"             │   │
│  │  上次执行: 周一 09:00 · ● 成功 · 2m 30s                  │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  memory_cleanup                        启用 ◉  [编辑 ▾] │   │
│  │  每周日 03:00  ·  默认模型                                │   │
│  │  "清理 inbox，归档过期记忆，更新 memory.md 索引"          │   │
│  │  上次执行: 上周日 03:00 · ● 成功 · 1m 12s                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  [+ 添加定时任务]                                               │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

#### 列表展示

每条定时任务一个卡片，四行信息：

1. **名称 + 操作**：名称左对齐，右侧是 Toggle 启用/停用 + 「编辑 ▾」下拉菜单（编辑 / 立即执行 / 删除）
2. **时间 + 模型**：Cron 表达式的人类可读翻译（如"每天 02:00"、"每周一 09:00"） + 使用的模型名。hover 时在翻译旁边用 tooltip 显示原始 cron 表达式
3. **指令内容**：引号包裹的指令文本，超长时单行截断 + 省略号
4. **上次执行**：时间 · 状态 · 耗时。状态色：成功 `emerald-400`，失败 `red-400`，运行中 `cyan-400` + 脉搏。点击跳转到对应 Session 回放。如果从未执行过显示 `尚未执行`（`slate-500`）

**Toggle 启用/停用**：即时生效，等价于注释/取消注释 crontab entry。停用后卡片整体降低不透明度（`opacity: 0.5`）。

**立即执行**：手动触发一次任务执行（等价于 Scheduler 立刻创建一个 Session），不影响后续定时调度。点击后在卡片底部显示 `⟳ 正在执行...`。

#### 添加定时任务

点击「+ 添加定时任务」，在列表底部展开内联表单：

```
┌─ 添加定时任务 ─────────────────────────────────────────────────┐
│  名称           [                    ]                          │
│  执行时间       [每天 ▾]  [02 ▾] : [00 ▾]                      │
│                 或手动输入 cron: [            ]                  │
│  指令           [                                            ]  │
│                 [                                            ]  │
│  模型           [默认模型 ▾]                                    │
│  ┄┄┄┄┄┄┄┄┄┄┄┄ 高级选项 (点击展开) ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄  │
│  任务重叠       [skip ▾]     跳过 / 排队 / 替换                 │
│  错过执行       □ 恢复后补跑一次                                │
│  超时           [          ] 秒    留空表示不限                  │
│                                                                 │
│                                            取消  ·  添加        │
└────────────────────────────────────────────────────────────────┘
```

**执行时间的双模式输入**：

- **可视化模式**（默认）：通过下拉组合选择频率（每天/每周/每月） + 具体时间。覆盖 80% 常见场景。选择后自动生成 cron 表达式，在下方灰色文字显示（如 `cron: 0 2 * * *`）。
- **Cron 模式**：点击"或手动输入 cron"切换为 cron 输入框。输入时实时翻译为人类可读文本显示在下方（如 `= 每天 02:00`）。格式错误时内联报错。

**指令**：多行文本框，高度自适应（最多 4 行后出现滚动条）。

**模型**：下拉选择已注册模型，默认选「默认模型」（使用系统当前活跃模型）。

**高级选项**：默认折叠——大多数定时任务用默认值就够了。点击展开显示任务重叠策略、错过执行策略、超时设置。

#### 编辑定时任务

点击卡片上「编辑 ▾」→「编辑」，卡片原位展开为和添加表单相同结构的编辑表单（预填当前值）。名称创建后不可修改。

#### 删除定时任务

点击「编辑 ▾」→「删除」，弹出确认对话框。如果任务正在执行中，提示用户任务将被中止。确认后从 config 和 crontab 中同时移除。

### Fuse List Tab

熔断名单管理，支持添加、删除和编辑。

```
┌─ Fuse List ────────────────────────────────────────────────────┐
│                                                                 │
│  熔断名单中的命令会被拦截，AI 无法执行。                          │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  rm -rf /                    系统根目录删除    [编辑] ×   │  │
│  │  rm -rf /*                   系统根目录删除    [编辑] ×   │  │
│  │  mkfs                        格式化磁盘       [编辑] ×   │  │
│  │  dd if=/dev/zero             磁盘覆写         [编辑] ×   │  │
│  │  :(){ :|:& };:               Fork 炸弹        [编辑] ×   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─ 添加 ──────────────────────────────────────────────────┐  │
│  │  命令模式:  [                    ]                        │  │
│  │  说明:      [                    ]                        │  │
│  │                                          + 添加           │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

- 每条规则显示命令模式和说明，hover 时右侧出现「编辑」和 `×`
- 「编辑」：该行原位切换为可编辑输入框（命令模式 + 说明），`Enter` 保存，`Esc` 取消
- `×` 删除：需确认。确认弹窗中强调"删除后 AI 将可以执行该命令"
- 底部添加表单始终可见。命令模式支持通配符（如 `rm -rf /` 会匹配 `rm -rf /Users/...`），输入时如果和已有规则重复，内联提示"该命令已存在"
- 熔断名单很少修改，保持朴素

---

## Metrics 页面

Dashboard 里有费用概览（聚合数字），Metrics 是展开版——时间维度趋势图和分维度明细。

### 页面结构

```
┌─ Metrics ──────────────────────────────────────────────────────┐
│                                                                 │
│  ┌─ 时间范围 ─────────────────────────────────────────────┐    │
│  │  7d  ·  30d  ·  90d  ·  自定义                         │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌─ 费用趋势 ─────────────────────────────────────────────┐    │
│  │  $                                                      │    │
│  │  2.0 ┤          ╭─╮                                     │    │
│  │  1.5 ┤    ╭─╮   │ │  ╭─╮                                │    │
│  │  1.0 ┤╭─╮ │ │╭─╮│ │  │ │╭─╮                             │    │
│  │  0.5 ┤│ │ │ ││ ││ │╭─╮│ ││ │                             │    │
│  │  0   ┼┴─┴─┴─┴┴─┴┴─┴┴─┴┴─┴┴─┴─── 日期                   │    │
│  │      2/20  21  22  23  24  25  26  27                   │    │
│  │                                                         │    │
│  │  ■ claude-opus  ■ claude-sonnet  ■ gpt-4o               │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌─ Token 用量 ───────────────┬─ 模型分布 ────────────────┐    │
│  │  (堆叠条形图               │  claude-opus    68%       │    │
│  │   Y 轴单位 k tokens)       │  █████████████░░░         │    │
│  │                             │  claude-sonnet  24%       │    │
│  │                             │  █████░░░░░░░░░░         │    │
│  │                             │  gpt-4o          8%       │    │
│  │                             │  ██░░░░░░░░░░░░░         │    │
│  └─────────────────────────────┴───────────────────────────┘    │
│                                                                 │
│  ┌─ 详细记录 ─────────────────────────────────────────────┐    │
│  │  日期        模型           输入    输出    缓存   费用   │    │
│  │  ────────────────────────────────────────────────────── │    │
│  │  2/27       claude-opus    85k    12k    28k   $1.89   │    │
│  │  2/27       claude-sonnet  42k     8k    15k   $0.31   │    │
│  │  2/26       claude-opus    92k    15k    35k   $2.04   │    │
│  │  ...                                                    │    │
│  └─────────────────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────────────┘
```

### 设计决策

**堆叠条形图**。用 recharts，Y 轴费用（$），X 轴日期。每个柱子按模型分色堆叠。模型颜色取品牌色的不同明度变体——都在 cyan 色域内，不引入新色调：

| 模型 | 颜色 |
|------|------|
| claude-opus | `cyan-400` (`#22d3ee`) |
| claude-sonnet | `cyan-600` (`#0891b2`) |
| gpt-4o | `cyan-800` (`#155e75`) |
| 其他 | `slate-500` |

同色系渐变，视觉统一，不会变成彩虹。

**Token 用量图**和费用趋势同结构，Y 轴单位变成 k tokens。两张图并排，用户可以对照"钱花在哪了"和"Token 消耗在哪了"。

**模型分布**用水平条形图，和 Dashboard 费用概览的条形图样式一致。

**详细记录表格**。底部按日期 × 模型的维度聚合，展示输入/输出/缓存/费用的明细。从 `metrics.db` 查出。Geist Mono，紧凑表格。

**时间范围**。7d / 30d / 90d 快捷选择 + 自定义。切换时图表平滑过渡（数据点 crossfade，不要整个图重绘闪烁）。

---

## Chat Drawer（Web Channel）

从右侧滑出的轻量对话面板。Web UI 的主要角色是观测和管理，对话是辅助功能，用抽屉保持上下文不丢失。

### 触发和布局

```
┌─ 主界面 ─────────────────────────────────────────┬─ Chat ────┐
│                                                    │           │
│  (Dashboard / Sessions / Memory / ...)             │  ┌─────┐ │
│                                                    │  │ 💬  │ │
│  ← 主内容区自动收窄                                │  │     │ │
│                                                    │  │ ... │ │
│                                                    │  │     │ │
│                                                    │  │ 输入 │ │
│                                                    │  └─────┘ │
└────────────────────────────────────────────────────┴──────────┘
                                                     360px
```

**触发方式**：侧边栏底部聊天图标按钮。点击后 Drawer 从右侧滑入（`cubic-bezier(0.16, 1, 0.3, 1)`，300ms），主内容区同步收窄。再次点击或 `Esc` 收起。

**宽度**：360px 固定。足够容纳一行中文或代码，但不过宽影响主内容。

**不遮挡**：Drawer 是推挤式（不是浮层）——主内容区收窄而不是被盖住。用户可以一边看 Dashboard 一边对话。

### Drawer 内部结构

```
┌─ Chat Drawer (360px) ────────────────────┐
│  ┌─ 头部 ──────────────────────────────┐ │
│  │  Web Channel  ·  claude-opus    ×   │ │
│  └─────────────────────────────────────┘ │
│  ┌─ 消息区 ────────────────────────────┐ │
│  │                                      │ │
│  │  系统通知（如果有）                   │ │
│  │  ┌────────────────────────────────┐  │ │
│  │  │ 📋 daily_update_check 完成     │  │ │
│  │  │ 未发现新版本                    │  │ │
│  │  └────────────────────────────────┘  │ │
│  │                                      │ │
│  │  用户: 刚才那个更新检查正常吗？       │ │
│  │                                      │ │
│  │  AI: 是的，今天凌晨 2 点的定时...     │ │
│  │                                      │ │
│  └─────────────────────────────────────┘ │
│  ┌─ 输入区 ────────────────────────────┐ │
│  │  [输入消息...              ] ↵ 发送  │ │
│  └─────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

**头部**：显示"Web Channel" + 当前模型名，`×` 关闭。极简。

**消息区**：对话消息和系统通知共存（和架构里的通知机制一致）。通知用卡片样式（浅色边框 + 图标），对话用和 Session 回放一致的气泡区分——用户消息有 `cyan-400` 左边框，AI 消息无边框。

**输入区**：单行输入框，`Enter` 发送，`Shift+Enter` 换行。高度自适应（最多 4 行后出现滚动条）。右侧发送按钮。

**流式响应**：AI 回复实时流入，和其他 IM Channel 行为一致。

**工具调用**：在消息区显示折叠的工具调用块，样式和 Session 回放一致——但因为空间小，默认只显示工具名和一行摘要。

**Session 管理**：Drawer 对应一个 `source: web` 的 Session。关闭 Drawer 不结束 Session，重新打开时恢复上下文。`/new` 命令创建新 Session。

### 排队消息反馈

所有 Channel（飞书、Telegram、Web Chat Drawer）对排队消息使用统一的反馈模式：

**即时确认**：用户发送消息后，如果 Agent 正忙（tool use loop 中），Channel 立即返回一条轻量确认。确认方式因 Channel 而异：

| Channel | 确认方式 | 说明 |
|---------|---------|------|
| 飞书 | 消息回复 "⏳ 已收到，任务处理中，稍后回复" | 用飞书的消息回复功能，关联到用户原消息 |
| Telegram | 消息回复 "⏳ 已收到，任务处理中，稍后回复" | 用 Telegram 的 reply_to_message |
| Web Chat Drawer | 消息气泡下方灰色提示 | 和 Session 详情页一致 |

确认消息用统一文案，保持简短。确认消息**不进入 Session 的 messages 数组**（它是 Channel 层的即时反馈，不是 Agent 的回复）。

**处理后通知**：Agent 回应排队消息后，回复通过正常的流式推送路径送达 Channel。在 IM 端，回复同样以 reply 形式关联到用户的排队消息，让对话线索清晰。

---

## 技术实现要点

### 后端（Hono）

API 路由以 `/api` 为前缀（详见 [TechStack - 命名规范]）：

```typescript
const api = new Hono()
  .get('/api/sessions', async (c) => c.json(await sessionStore.listActive()))
  .get('/api/sessions/:id', async (c) => c.json(await sessionStore.get(c.req.param('id'))))
  .get('/api/memory/search', async (c) => c.json(await memory.search(c.req.query('q'))))
  .put('/api/memo', async (c) => { await memo.update(await c.req.text()); return c.json({ ok: true }) })
```

### 实时推送（WebSocket）

WebSocket 协议和事件格式详见 [TechStack - 实时通信架构]。以下为简化示例：

```typescript
// 全局事件总线
const bus = new EventEmitter()

// Session 状态变化时
bus.emit('session:update', { id, state, currentTool })

// 工具调用时
bus.emit('tool:call', { sessionId, tool, input, output })

// WebSocket handler 订阅事件
bus.on('session:update', (data) => ws.send(JSON.stringify(data)))
```

### 前端（React + Tailwind）

完整的前端技术选型和目录结构详见 [TechStack - Web UI 技术栈] 和 [TechStack - 前端目录结构]。

- Vite 构建
- recharts 图表
- CodeMirror Markdown 编辑器
- 前端 build 后静态文件由 Hono 直接 serve

### 费用计算

模型价格不额外存储，从 [litellm/model_prices_and_context_window.json](https://github.com/BerriAI/litellm/blob/main/model_prices_and_context_window.json) 默认值或 `config.yaml` 覆盖值获取，单位为 `$/M tokens`。每条请求的实际费用在写入对应 `llm_request` trace span 时已根据 token 用量和模型价格计算并记录（详见 [Architecture - 模型定价与用量追踪]）。

```typescript
// TokenUsage 和 ModelPricing 类型定义详见 [TechStack - Provider Adapter 接口]
function calcCost(tokens: number, pricePerM: number): number {
  return (tokens / 1_000_000) * pricePerM
}

function calcRecordCost(usage: TokenUsage, pricing: ModelPricing): number {
  const perM = (tokens: number, price?: number) =>
    price ? (tokens / 1_000_000) * price : 0
  return (
    perM(usage.input, pricing.input) +
    perM(usage.output, pricing.output) +
    perM(usage.cacheWrite ?? 0, pricing.cacheWrite) +
    perM(usage.cacheRead ?? 0, pricing.cacheRead) +
    perM(usage.reasoning ?? 0, pricing.reasoning)
  )
}
```

---

## 设计原则总结

1. **安静时简洁，有事时醒目** — Dashboard 动态布局，正常时不显示待办卡片
2. **信息密度适中** — 不是极端高密度，有呼吸感的间距
3. **材质感** — 磨砂玻璃、极细边框、微光 hover，不是平面色块
4. **有意义的运动** — 每个动画都传达信息（渐次浮现引导视线、脉搏表示活跃、数字滚动表示消耗）
5. **类型系统** — 颜色绑定类型（incident=红、runbook=绿），全站统一
6. **关联可导航** — Memory 的关联记忆跳转、Session 的来源链接，形成知识网络
7. **实时感知** — WebSocket 推送，Dashboard 事件流、活跃 Session 工具调用、实时 Session 回放都是动态的
8. **键盘友好** — Session 回放、Memory 浏览都支持键盘导航
