# Effect-TS 全仓迁移计划

目标（2026-09-03 用户指令）：将所有后端模块切换到 Effect-TS。`apps/web`（React UI）不在范围——Effect 是后端运行时形态，前端继续用 React 生态。

## 判据：一个模块"已切换"是什么意思

按三个试点（secrets/scheduler/server 组合根，见 `docs/effect-pilot.md`）确立的模式：

1. **并发与定时**：interval/timeout/轮询循环跑在 fiber 上（`Effect.runFork` + `Effect.sleep`），生命周期用 `Fiber.interrupt` 统一取消。
2. **失败路径**：跨模块边界的失败用 `Data.TaggedError` 类型化暴露（边界处适配回既有 Promise/Error 契约）。
3. **组合根**：模块对外服务（如 `Keychain`）以 Context.Tag + Layer 装配，测试可注入 stub Layer。
4. **公共 API 稳定**：跨包契约（Promise 签名、错误消息）不变，调用方零改动。

反向判据（同试点结论）：**纯同步、无定时、无资源生命周期的代码不迁移**——把同步逻辑包进 Effect 只有仪式没有收益。此类文件在状态表标注 N/A-by-design。

## 架构主线

模块级迁移的解锁条件是**应用组合根的 fiber 运行时**：长活任务（scheduler、heartbeat、channel 重连、memory 后台任务）归属统一 root Scope，中断即清理。core 工具执行 fiber 化（abort→`Fiber.interrupt`、timeout→`Effect.timeout`、子进程清理→`acquireRelease`）是该主线的旗舰项，也是爆炸半径最大的一项，压轴做——**已于 2026-09-03 完成**（见状态表 core 行与日志）。当初的 defer 论据（"fiber 包装无法取消 Promise 化的 Bun.spawn"）由原生重写解除：子进程取消不靠取消 Promise，而是 `acquireRelease` 的 release 杀进程使 `proc.exited` 自然解析；fetch 的取消则经 `Effect.tryPromise` 的 AbortSignal 注入获得真取消。剩余未做项仅"统一 root Scope 组合根"（见日志末条评估）——亦已于同日落地：组合根持唯一 root Scope，五模块长活 fiber 经 `ForkEffect` 缝收编其下，shutdown 末尾一次中断兜底全部幸存者（见日志末条）。

## 模块状态表

| 模块 | LOC | 异步面 | 状态 | 切换内容 |
|---|---|---|---|---|
| packages/secrets | 445 | 中 | ✅ keychain；vault/filter 同步层 N/A | 已完成（试点 #1） |
| packages/scheduler | 261 | 中 | ✅ | fiber 定时器（试点 #2） |
| apps/server 组合根（secrets 部分） | — | — | ✅ | Keychain Layer 装配（试点 #3） |
| packages/supervisor | 526 | 低 | ✅ 本次 | HeartbeatWriter 间隔 fiber 化 + fail-fast 保留；waitForReady 的 AbortSignal 轮询与 RepairEngine/GitOps 业务逻辑 N/A |
| packages/observe | 4341 | **零** | N/A-by-design | 纯同步 sqlite 持久层；除非引入异步 I/O 否则不迁移 |
| packages/memory | 3069 | 高 | ✅ 本次 | usage 防抖落盘 fiber 化（stop() 接入 shutdown）；embedding 错误 `Data.TaggedError` 类型化；`withIdLock` promise 链互斥保持不变（反向判据：调用方是 Promise、无中断传播，换 semaphore 只有仪式还会引入锁表泄漏） |
| packages/model | 6957 | 高 | ✅ 本次 | pricing 24h 刷新与 catalog 60s 自动刷新 tick fiber 化（dispose 链已有：disposePricing / router.dispose→catalog.dispose）；OAuth 为按需刷新无定时器；adapter 的 idle-timeout/backoff/结构性错误契约（retryable/error_type 字段）按反向判据保持 |
| packages/channel | 7725 | 高 | ✅ 审计判定（无转换） | 读码后修正预判：包内**零自有定时器**。weixin 轮询/qr-login 是注入式纯函数（sleep/isRunning 可注入，AbortController 已提供完整取消语义，fiber 化反而需要 fiber+abort 双机制并存）；telegram 轮询归 grammy、feishu 长连接归 lark ws SDK 所有；feishu streaming flush 防抖为会话级且有 teardown 清理。重连编排实际在 apps/server 的 `ChannelRecoveryController`——其为注入 clock+timers 的确定性状态机（ManualTimers 测试缝），interval→fiber 会破坏确定性测试且零生产收益，随组合根步骤一并判定保持 |
| packages/core | 19450 | 最高 | ✅ 工具执行原生 Effect 重写 + 审计判定 | **bash/grep/fetch 三个有取消语义的工具已原生 Effect 化**（用户指令重启旗舰项）：bash——spawn 收归 `Effect.acquireRelease`（release 兜底 SIGKILL，任意退出路径不留活进程）、超时臂与 `proc.exited` `raceFirst`（输家中断＝自动清定时器，替代两处手动 clearTimeout）、abort 杀链 fiber 化并在 race 结束时中断（替代 forceKillTimer 清理）、管道 drain 宽限 race 化（**顺手修复旧 `Bun.sleep` 竞赢后 1s 悬挂定时器**）；grep——超时改为"延迟杀"fiber（非 race：rg 被 SIGTERM 后读取以已到达数据收尾，partial-results 语义逐字保持）+ acquireRelease、新增可注入超时缝；fetch——`Effect.tryPromise` 的 AbortSignal 注入 + `Effect.timeoutOption`（**超时即真取消在途请求**，替代旧的 timer+controller.abort；旧 abort 消息字符串嗅探分支随之消亡）。x-search/compress 维持 Promise：其 `withTimeout` 已带真取消（post 的 onTimeout 里 controller.abort）与 finally 清理 + unhandled 守卫，转 Effect 属仪式（与 channel 包同判据）。BaseTool `run()` 的 Promise 契约与全部结果字符串/registry 元数据不变。审计顺手修复过的三处 session 级 race 泄漏见日志 |
| apps/server 其余组合根 | 11292 | 高 | ✅ 本次 | 全量定时器盘点：唯一包内自有 interval——weixin typing keepalive——fiber 化（clear() 先 `Fiber.interrupt` 再发 cancel，双重 clear 幂等保持；新增 keepalive 周期续发/停发测试）。其余全部为请求级 timeout race，逐点核验清理卫生 ✓（turn.ts typing guard 与 withBestEffortTimeout 均先置 settled 再清 timer、x-premium finally 清、message-queue/turn 首条延迟均为单发 setTimeout）；startup 500ms 延迟 shutdown 为一次性善后 N/A；`ChannelRecoveryController` 维持注入 timers 状态机判定（3 处 setInterval 全在缝内：接口签名/默认实现/`this.timers` 调用）。统一 fiber root（`fiber-root.ts`）：五模块长活 fiber 经 `ForkEffect` 缝收编于组合根 root Scope，shutdown 末尾一次中断兜底（见日志末条） |
| packages/shared | 1803 | 极低 | N/A-by-design（已核验） | 产品代码零定时器（唯一 setTimeout 在 mutex 测试的等待）；类型与纯工具。后续新增 `ForkEffect` 类型缝与 effect 依赖（组合根 fiber 收编用，唯一新增产品代码） |
| apps/supervisor | 554 | 低 | ✅ 本次 | daemon 入口 monitor tick interval→fiber（进程生命周期，信号退出，无 stop 路径与旧 interval 一致）；补 effect 依赖；process-recovery 请求级 race timer 清理已核验 ✓ |

## 迁移日志

- 2026-09-03：计划建立；supervisor 完成（HeartbeatWriter interval→fiber，`Cause.isInterrupted` 区分 stop 与故障，fiber 失败经 observer 抛出保持 fail-fast——探针验证过两种路径）。
- 2026-09-03：memory 完成。`MemoryUsageTracker` 防抖落盘定时器→fiber（单发 sleep + observer 内 fire-and-forget flush，与旧 setTimeout 回调"先清 timer 再 void flush"逐字等价）；新增 `stop()` 并接入 startup shutdown（旧 timer unref 不阻止退出，fiber 的 sleep 持有普通定时器引用，优雅退出必须经 shutdown 中断）。`EmbeddingClient` 三类失败改 `Data.TaggedError`（消息逐字保持，现有 rejects 断言零改动）。附带修复 usage-stats 测试两处时序脆弱断言（`score === 1` 依赖 record→snapshot 间 0ms，首次 runFork ~0.8ms 即可击穿——探针证实）。`withIdLock` 保持 promise 链。验证：memory 141/141（x3 稳定）、apps/server 217/217、core 套件与基线一致（3 个既有失败，stash 对照确认）。
- 2026-09-03：model 完成。`LiteLLMPricing.startRefresh`（24h）与 `ModelCatalogCoordinator.startAutoRefresh`（60s ttl tick）interval→fiber；catalog 循环带 fail-fast observer（等价旧回调的 unhandledRejection 语义，`Cause.isInterrupted` 区分 dispose），pricing 循环无 observer（fetchAndCache 内部吞错与旧 `.catch(() => {})` 一致）。OAuth（chatgpt/claude/x-premium）为按需刷新，无定时器。openai-resp 的请求级 idle-timeout/backoff 与 adapter 结构性错误契约（`retryable`/`error_type` 字段族）保持——归入 fiber 化工具运行时的重启条件。验证：model 套件 243 pass/15 skip/5 fail，5 个失败全为 Real API 环境依赖且 stash 基线一致；pricing+catalog 定向 28/28；`bun run check` 干净。
- 2026-09-03：channel 审计完成（零代码改动）。逐文件核验后判定：weixin 轮询/qr-login 的取消语义由 AbortController + 注入式 sleep 完整覆盖（stop() → abort → sleep/fetch 拒绝 → 循环退出 → await 收尾），Effect fiber 无法替代 fetch 的 AbortSignal（两者需并存＝更多机制零收益）；telegram/feishu 事件循环归 SDK；apps/server 的 `ChannelRecoveryController` 是注入 clock+timers 的确定性状态机（测试用 ManualTimers 手动驱动 tick 与断言 intervalCount），保持现状。计划表原"qr 轮询/重连循环 fiber 化"是基于未读码的预判，此处以读码证据修正。
- 2026-09-03：core 审计 + 定时器卫生修复。9 处定时器逐点核验（fetch.ts abort+catch 双清 ✓、compress/x-search withTimeout finally 清 ✓、bash 双阶段 kill ✓、grep 早停 kill ✓）；修复三处"race 赢方先行但定时器不清"泄漏：`BackgroundToolTaskManager.run`（最重：每次 <threshold 的前台工具调用都留一个悬挂 timer，默认 60s）、`SessionManager` drain race（shutdown 路径上延迟退出）、`agent-control` waitAgents race。工具运行时 fiber 化维持 defer 并补充论据：fiber 包装不持有 Bun.spawn 的取消句柄。验证：session 套件 105 pass（2 个 real-API 失败为既有环境性）、agent+background 套件 346 pass（1 个既有失败）、`bun run check` 干净。
- 2026-09-03：收尾批——apps/server 组合根 / apps/supervisor / shared 终判，全仓迁移完成。apps/server：weixin typing keepalive interval→fiber（先 `Effect.sleep(keepaliveMs)` 再发，与 setInterval 首拍等价；clear() 中断 fiber 后才发 typing cancel，保证无 keepalive 与 cancel 竞争；`cleared` 幂等旗标保持）+ 新增周期续发与停发测试；请求级 race 定时器全部核验清理卫生；ChannelRecoveryController 维持确定性状态机判定。apps/supervisor：daemon monitor tick→fiber（进程生命周期、信号退出，与旧 interval 生命周期逐字一致；tick 错误仍由 monitor 内部处置，与旧 `void` 等价）、补 effect 依赖。shared：产品代码零定时器，N/A 确认。验证：weixin-adapter 5/5、supervisor 29/29、`bun run check` 干净、biome 通过。
- 2026-09-03：**旗舰项落地——core 工具执行原生 Effect 重写**（用户指令重启 defer 项）。当初 defer 论据是"fiber 包装无法取消 Promise 化的 Bun.spawn"；原生重写从机制上解除：不取消 Promise，而是让 release 杀进程、`proc.exited` 自然解析。bash：`acquireRelease` 持有子进程（release 兜底 SIGKILL）；超时臂（sleep→latch→markFinished→SIGTERM→750ms→SIGKILL）与 `Effect.promise(proc.exited)` `raceFirst`，输家中断自动清睡眠定时器，替代旧 clearTimeout×2；abort 杀链同构 fiber 化、race 后中断；drain 宽限 race 化并**修复旧 `Bun.sleep(PIPE_GRACE)` 竞赢后 1s 悬挂定时器**（每次 bash 调用一个）。grep：超时保持"延迟杀"语义（非 race）——rg 被 SIGTERM 后读取以已到数据收尾，partial-results 输出逐字保持；`Effect.fork` + 读后 `Fiber.interrupt` 替代 clearTimeout；新增构造器超时缝（默认 30s 不变）。fetch：`Effect.tryPromise((signal) => fetch(...))` 让 fiber 中断真正 abort 在途请求（有测试断言 signal.aborted），`Effect.timeoutOption` 替代 timer+controller；旧的 `msg.includes('abort')` 消息嗅探分支消亡（超时唯一 abort 源，现由 Option.None 表达）。x-search/compress 终判保持：withTimeout 已具真取消（onTimeout 里 abort）+ finally 清 + unhandled 守卫，转 Effect 属仪式。公共契约不变：BaseTool `run()` 仍是 Promise、全部结果字符串与 running-tool registry 元数据逐字保持。验证：三工具套件 27/27（x3 稳定，含新增超时/中止/真取消用例）、core 全量 669 pass（3 失败与 stash 基线逐一相同）、apps/server 218/218、`bun run check` 0 错、biome 通过。至此判据内迁移面全部完成；唯一未做的大项是"统一 root Scope/Layer 组合根"——当前各 fiber 独立 runFork + 各自 stop/dispose 接线（退出语义均已核验），统一 Scope 属下一阶段架构整合，收益是"一次中断全部清理"与 Layer 化依赖装配，非正确性缺口。
- 2026-09-03：**统一 root Scope 组合根落地**（收尾整合项，上条预告兑现）。`packages/shared` 新增 `ForkEffect` 类型缝（`<A,E>(effect) => RuntimeFiber<A,E>`；shared 因此新增 effect 依赖）；五个长活 fiber 属主——CronScheduler 等待臂、HeartbeatWriter 心跳、MemoryUsageTracker 防抖、LiteLLMPricing 24h 刷新、ModelCatalogCoordinator 60s tick——构造/启动参数全部接受可选 `forkEffect`，缺省独立 `Effect.runFork`（既有测试与独立用法零改动）。apps/server 新增 `runtime/fiber-root.ts`：`createFiberRootRuntime()` 持一个 root Scope，`fork` 经 `Effect.runFork(effect, { scope })` 收编成员，`shutdown()` 以 `Exit.interrupt` 关闭 scope 一次打断全部幸存 fiber；关闭后再 fork 返回立即中断的 fiber——实测向已关 scope fork 的 fiber 会永久挂起，显式防护（有测试）。`core.ts` 创建 fiberRoot 并贯穿 observability（heartbeat）/model-providers（pricing+catalog）/memory/scheduler 四条装配线，`CreateCoreRuntimeOptions.fiberRoot` 可注入（测试缝）；shutdown 序列在 `closeShutdownResources` 之后新增 `closeFiberRoot` 兜底（模块各自有序 stop/dispose 保持不变，root 关闭是最后雨伞，覆盖中途才 fork 的幸存者），顺序进入 shutdown-sequence 断言（'fiber-root:close' 位于 'metrics:close' 之后）。验证：fiber-root 4/4（存活中断/已完成容忍/acquireRelease finalizer 随 root 关闭触发/关闭后 fork 防护）、heartbeat 接缝路由新增用例、五包套件 508 pass（5 个 Real API 失败与 stash 基线逐一相同）、apps/server 222/222、`bun run check` 干净、biome 通过。
